# Шаг 6. Поиск: почему медленно и что с этим делать

## Что нужно сделать

Каталог дорос до ста тысяч товаров, и поиск по названию начал тормозить. Шаг
состоит из трёх частей, и главное в нём — числа, а не код.

**Первое: измерить.** Наполни базу и сними время ответа — как это сделать,
написано в `bench/README.md`. Посмотри план запроса через `explain analyze`.
Запиши время до правок: без него не с чем будет сравнивать.

**Второе: индекс.** Поиск идёт по части названия — `LIKE '%…%'`. Обычный
btree-индекс, который уже стоит на `title`, здесь не работает. Заведи миграцией
триграммный gin-индекс и сними время ещё раз. Учти, что тесты гоняются на H2,
а такого индекса там нет: у `changeSet` есть атрибут, который ограничивает его
одной СУБД.

**Третье: кэш.** Карточку товара открывают чаще всего, и почти всегда одну и ту
же — популярную. Включи кэширование, положи карточку в Redis и сбрасывай запись
при любом изменении товара: цена, скидка, остаток, резерв.

## Где править

`// TODO шаг 6` в `CatalogStarterApplication`, `ProductService`,
`application.yml`, `db.changelog-master.yaml`.

Стенд с Redis:

```bash
docker compose -f ../../infra/compose.yaml up -d postgres-catalog redis
```

## Как проверить себя

```bash
../../gradlew :catalog-starter:test
```

`ProductCacheTest` считает обращения к базе: три подряд запроса карточки должны
дать ровно одно обращение. Два других его теста сейчас зелёные — они начнут
падать, если кэш появится, а сброс нет: покупатель увидит старую цену. Тесты
идут на кэше в памяти, Redis для них поднимать не нужно — кэш подменяется
настройкой, а код от этого не меняется.

Числа из `bench/measure.sh` — до индекса, после индекса, первый и повторный
запрос карточки — принеси в ответе на задание.

## На что посмотреть по дороге

- Кэш без сброса — это не ускорение, а тихая порча данных. Сначала сброс, потом
  ускорение.
- Время жизни записи: сутки, десять минут или минута — это решение, а не
  умолчание. От чего оно зависит?
- Кэш не спасает поиск по каталогу: у каждого запроса свой текст, попадания
  почти не будет. Поэтому индекс и кэш решают разные задачи, а не одну.

## Материал

- Типы индексов: https://vikulin-va.ru/postgres/indexes-types/
- Паттерны кэширования: https://vikulin-va.ru/redis/caching-patterns/
- Алгоритмы: https://vikulin-va.ru/algorithms/
