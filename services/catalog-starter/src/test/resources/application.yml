spring:
  datasource:
    url: jdbc:h2:mem:catalog;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false
  cache:
    type: simple

# TODO шаг 15: те же настройки наблюдаемости нужны и в тестовом профиле —
# тестовый application.yml перекрывает основной целиком.
