server:
  port: 8082

spring:
  application:
    name: catalog-starter
  datasource:
    url: jdbc:postgresql://localhost:5439/catalog_starter
    username: catalog
    password: catalog
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  # TODO шаг 6: кэш на Redis. Хост и порт — из стенда infra/compose.yaml,
  # время жизни записи выбираешь сам и объясняешь, почему столько.
