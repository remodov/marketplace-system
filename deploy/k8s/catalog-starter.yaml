apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-starter
  labels:
    app: catalog-starter
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-starter
  template:
    metadata:
      labels:
        app: catalog-starter
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: app
          image: ghcr.io/remodov/catalog-starter:0.1.0
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres-catalog-starter:5432/catalog_starter
            - name: REDIS_HOST
              value: redis
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-starter
spec:
  selector:
    app: catalog-starter
  ports:
    - port: 80
      targetPort: 8082
